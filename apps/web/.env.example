# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3001

# Better Auth Configuration
# The API URL where Better Auth endpoints are hosted
# In production, set this to your API domain (e.g., https://api.yourdomain.com)
