/**
 * Re-export TanStack Store with starter-specific utilities
 */
export { Store, useStore } from '@tanstack/react-store';

/**
 * Example: Theme type
 */
export type Theme = 'light' | 'dark' | 'system';
